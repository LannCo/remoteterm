# RENAME_ALLOWLIST.md

Pre-sed check for Phase 3 step 9 (`RENAME_PLAN.md`), recorded per that step's instruction to
persist this as a committed artefact rather than a one-off grep. Command run:

```
git ls-files -z '*.go' '*.mod' '*.tmpl' '*.json' | xargs -0 grep -n "wavetermdev/waveterm"
```

218 hits across 205 `.go`, 10 `.mod`, 2 `.tmpl`, 1 `.json` file.

## Comment-only attribution references

```
git ls-files -z '*.go' '*.mod' '*.tmpl' '*.json' | xargs -0 grep -n "wavetermdev/waveterm" \
  | grep -E "^\S+:[0-9]+:\s*(//|/\*|\*)"
```

Returned no matches. No genuine "this behaviour was copied from / attributed to the upstream
repo" comment exists in the scanned file set. Nothing excluded on this basis.

## Excluded from the module-path sed

- **`schema/settings.json`'s `$id` field** (`https://github.com/wavetermdev/waveterm/pkg/wconfig/settings-type`):
  generated by `task build:schema` (a dependency of `task generate`). Not hand-edited; it
  self-corrects when regenerated after the `pkg/wconfig` -> `pkg/rtconfig` package rename and the
  module path change, and is regenerated + committed in this same Phase 3 commit via `task
  generate` per the verification gate.
- **`pkg/wconfig/defaultconfig/settings.json`'s `web:defaulturl`**: already rebranded to
  `https://remoteterm.dev` in Phase 1 step 2. No `wavetermdev`/`waveterm` string remains in this
  file; confirmed by direct read before this pass.
- **`tsunami/demo/githubaction/app.go:31`**: `app.ConfigAtom("repository", "wavetermdev/waveterm", ...)`.
  This is a demo app's illustrative default config value (a GitHub Actions status-checker demo,
  defaulted to point at a real, active external repo as a working example), not a project
  self-reference. It does not match the `github.com/wavetermdev/waveterm` sed pattern used below
  (no `github.com/` prefix in the literal), so it is naturally excluded rather than needing an
  explicit carve-out in the sed invocation. Left unchanged.
- **`tsunami/demo/*/go.mod` (8 files: cpuchart, githubaction, modaltest, pomodoro, recharts,
  tabletest, todo, tsunamiconfig)**: reclassified per `RENAME_PLAN.md` Prerequisites as
  cosmetic-only and unverifiable — each already carries a dead `replace` directive pointing at an
  upstream developer's local machine path, so nothing in the module graph or any gate in this plan
  builds them. Their module-path strings were rewritten anyway in this pass for consistency
  (harmless, since nothing depends on them compiling), but this is **not** treated as satisfying
  any compile obligation for Phase 3's verification gate.

## Everything else

The remaining hits (root `go.mod`, `tsunami/go.mod`, all in-scope `*.go` files including the two
root-module files that import `tsunami` from outside it, and the two `tsunami/templates/*.go.tmpl`
files) are in scope for the `github.com/wavetermdev/waveterm` -> `github.com/LannCo/remoteterm`
rewrite, including the `/tsunami` suffix case (tsunami renames with the root module per
Prerequisites decision 4 — the same prefix substitution covers both correctly since the target is
literally the old prefix with `/tsunami` appended).
