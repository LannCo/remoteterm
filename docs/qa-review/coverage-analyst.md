# coverage-analyst report
**Target:** Test-coverage gaps for new/changed logic on `qa/fleet-2026-09-22` vs `origin/main` (remotetermconfig-model, generalcontent, data-dir migration shim, jobcontroller stream-health, conncontroller thresholds, JWT/swap-token redaction, review-mode diff-cache)
**Started:** 2026-09-22T00:00:00Z (session clock)
**Status:** IN PROGRESS

## Covered well
- `hasActiveStream` and `registerNewJobStream` (jobcontroller.go:1489-1508): direct unit tests at `pkg/jobcontroller/jobcontroller_test.go:1383` (`TestHasActiveStream`) and `:1427` (`TestRegisterNewJobStreamSeedsHealthSynchronously`); both assert the exact synchronous-seed behaviour from commit 58bad6e2.
- `getReconnectConfig` (jobcontroller.go:198, 94.1% per `go tool cover -func`): `TestGetReconnectConfigDefaults`/`Overrides`/`IgnoresNonPositiveOverride` at jobcontroller_test.go:845,865,892.
- conncontroller reconnect/monitor threshold config (`ace80192`): thoroughly covered — `TestGetIntConfigDefaultsWhenUnset`/`Overrides`/`IgnoresNonPositiveOverride` (conncontroller_test.go:349,364,386), `TestGetTickerIntervalCapsAtOneSecond` (:405), `TestGetStallDisconnectThresholdMsDefault`/`FromConfig` (:226,239), `TestShouldAutoDisconnectOnStallDefault`/`RespectsConfig` (:257,269), `TestCheckConnectionRespectsConfiguredKeepaliveInterval`/`StallThreshold` (:427,455). connmonitor.go functions mostly 75-100%.
- `onConnectionUp` retry/no-starvation behaviour: `TestOnConnectionUpPerJobCtxNoStarvation`, `TestOnConnectionUpRetryRecoversFailedJobs`, `TestOnConnectionUpRetryAbortsOnConnDown`, `TestOnConnectionUpRetrySkipsDoneJobs` (jobcontroller_test.go:590,655,732,784) — 79.8% func coverage.
- Review-mode diff-cache invalidation (`518ef5b9`): `jumpToFile` block:"nearest" fix directly asserted at `frontend/app/view/sourcecontrol/review-mode.test.ts` ("jumpToFile > calls scrollIntoView when ref exists"); cache-key/invalidation fix covered by "fetchDiffCached > uses different cache keys for staged vs unstaged" and "invalidateDiffCache > removes all cache entries for a path". `sourcecontrol-model.ts` measured at 219/388 lines (56.4%), 33/57 funcs (57.9%) via `npx vitest run --coverage` (istanbul, lcov).
- `generalcontent.tsx` search/field mapping (`matchesFieldSearch`, `filterFieldSchemas`): 8 direct unit tests at `generalcontent.test.ts:16-52` covering empty-query passthrough, label/key/description case-insensitive matching, no-match rejection, and category-narrowing. This is the only part of generalcontent.tsx exercised (3/35 functions overall) but it is the part in scope.

## Gaps ranked
<!-- appended one at a time, as found. IDs COV-n -->

### COV-1 [HIGH] handleRouteEvent has zero test coverage — the actual recovery path for failure mode B
- **Path:** `pkg/jobcontroller/jobcontroller.go:558-606` (`handleRouteEvent`, called by `handleRouteUpEvent`/`handleRouteDownEvent`). `go tool cover -func` reports 0.0% for `handleRouteEvent`, `handleRouteUpEvent` (not separately listed — trivial wrapper, same story), and `handleRouteDownEvent`.
- **What's uncovered:** the whole route-up branch that decides whether to trigger `ReconnectJobRoute` when a route comes up with no active stream (lines 573-597), including the `hasActiveStream` check, the singleflight-guarded async restart goroutine, and the route-down auto-reconnect trigger (lines 599-603, `shouldAttemptAutoReconnect`/`attemptAutoReconnect` dispatch).
- **Why it matters:** this is the exact "Path 2" recovery route called out in commit 91814977's message (route re-registers independently of `doReconnectJob`). `hasActiveStream`/`registerNewJobStream` are unit-tested (see Covered well), but nothing exercises `handleRouteEvent` itself — a regression that broke the `!hasActiveStream(...)` branch, the goroutine's panic-recovery wrapper, or the status-write-before-lookup ordering (line 563 sets status before line 566 fetches the job) would pass the full suite.
- **Suggested test:** call `handleRouteEvent(&wps.WaveEvent{Scopes: []string{"job:" + jobId}}, JobConnStatus_Connected)` with a seeded job in DB and (a) no `jobStreamHealth` entry — assert `ReconnectJobRoute` gets invoked (needs a way to observe it, e.g. via a test hook or by checking `jobStreamIds`/status side effects after the goroutine completes), and (b) an active-stream entry — assert no restart fires. Existing test infra in this file (see setup helpers used by `TestOnConnectionUpPerJobCtxNoStarvation`) already builds fake jobs/DB state, so the fixture cost is low.
- **Estimated effort:** M (needs to either wait on the async goroutine or refactor a synchronous hook point for testability).
- **Certainty:** traced to `go tool cover -func` line for `pkg/jobcontroller/jobcontroller.go:558` and confirmed no `handleRouteEvent`/`handleRouteUpEvent`/`handleRouteDownEvent` reference exists in jobcontroller_test.go (grep, zero hits).

### COV-2 [HIGH] doReconnectJob's Connected-but-no-stream restart branch is untested
- **Path:** `pkg/jobcontroller/jobcontroller.go:2012-2139` (`doReconnectJob`), 4.3% function coverage per `go tool cover -func`.
- **What's uncovered:** specifically the failure-mode-B branch at lines 2018-2037 — `CheckJobConnected` succeeds but `hasActiveStream` is false, triggering `restartStreaming(ctx, jobId, true, rtOpts)` and, on failure, forcing the job back to `JobConnStatus_Disconnected` (line 2035) so it's retried rather than wedged. Also uncovered: the "restartStreaming succeeds only then mark Connected" ordering fix at lines 2120-2138 (the bug this whole commit exists to fix — previously status was set Connected before the restart, which could wedge the job Connected-with-no-stream on a failed restart).
- **Why it matters:** this is the core bug fix from `91814977`'s "Layer 3" description. A regression that reordered these two writes, or that let the restart-failure path silently skip setting `Disconnected`, would re-introduce the exact wedged-terminal freeze from issue #25 with no test catching it. The only nearby coverage (`TestOnConnectionUpRetry*`) exercises `onConnectionUp`'s retry loop, not `doReconnectJob`'s internal branch logic directly.
- **Suggested test:** two table cases against `doReconnectJob` — (1) job connected, `jobStreamHealth` inactive, `restartStreaming` succeeds (via whatever stub/interface point `TestRestartStreamingClosesPrevReader` at jobcontroller_test.go:1217 already uses) — assert status stays/becomes Connected and no reconnect RPC was sent; (2) same but `restartStreaming` fails — assert status is set to Disconnected. A third case for the not-connected path (lines 2039-2138, sends `RemoteReconnectToJobManagerCommand`) is lower priority since it's more integration-shaped.
- **Estimated effort:** M — the function pulls in `wshclient.GetBareRpcClient()` and DB access; needs the same fixture pattern as `TestRestartStreamingClosesPrevReader`/`TestRunOutputLoopExitsOnReaderCloseWithSupersession` to fake or bypass those without a live connection.
- **Certainty:** traced to `go tool cover -func` line for `jobcontroller.go:2012`; confirmed zero references to `doReconnectJob` outside a stale comment at jobcontroller_test.go:327-329 noting it's *not* being intercepted.

### COV-3 [MEDIUM] shellexec.go and remotetermbase (wavebase.go) have zero test files — JWT redaction and dual-write fallback untested by any Go test
- **Path:** `pkg/shellexec/shellexec.go` (whole package, no `_test.go` file exists), `pkg/remotetermbase/wavebase.go` (same, no test file). `go test -cover` reports `coverage: 0.0% of statements` for both packages because there is nothing to run.
- **What's uncovered:** the redaction changes from `bb79c27d` (StartWslShellProc/StartRemoteShellProc: unconditional `log.Printf`/`Infof` of assembled commands and swap tokens downgraded to `Debugf`-gated, redacted output) and the JWT dual-write/fallback changes from `d6666d49` (`LegacyWaveJwtTokenVarName` dual-write at every shellexec.go export site, `wavebase.GetMapValNewOrLegacy` for map-based reads of a durable shell's persisted `SwapToken.Env`).
- **Why it matters:** this is security-sensitive logging code — a regression that reverted a `Debugf` back to `Infof`/`Printf`, or that dropped the redaction call, would leak a year-valid JWT or swap token into `waveapp.log`, which users attach to bug reports verbatim. There is no automated signal that would catch that regression; it would only surface via manual log inspection. Similarly, `GetMapValNewOrLegacy`'s fallback-to-legacy-key behaviour (needed for durable shells whose persisted env only carries the old `WAVETERM_JWT` key) has no test proving the fallback actually fires when only the legacy key is present.
- **Confirmed helper:** `shellutil.RedactSecret` at `pkg/util/shellutil/tokenswap.go:99-103` (called from shellexec.go:271,289,292,394,471,578,660 and `pkg/blockcontroller/shellcontroller.go:426`). Zero test files reference it (`grep -rln RedactSecret --include=*_test.go .` returns nothing), and `pkg/util/shellutil` only has `shellquote_test.go` — `tokenswap.go` itself is untested.
- **Suggested test:** a direct unit test of `shellutil.RedactSecret` (pure function, first-8-chars-plus-length per its own doc comment) asserting the full secret never appears as a substring of the output for representative JWT/swap-token-shaped inputs, including short/empty-string edge cases. For wavebase.go, a direct unit test of `GetMapValNewOrLegacy` with (a) only new key present, (b) only legacy key present, (c) both, (d) neither.
- **Estimated effort:** S for both `RedactSecret` and `GetMapValNewOrLegacy` — both are pure functions, cheap to isolate.
- **Certainty:** traced to `go test -cover` output (`coverage: 0.0% of statements` for both packages), `find pkg/shellexec pkg/remotetermbase -name "*_test.go"` returning no results, and `git show bb79c27d -- pkg/shellexec/shellexec.go` confirming the `RedactSecret` call sites.

### COV-4 [MEDIUM] cmd/server/main-server.go grabAndRemoveEnvVars has zero test coverage
- **Path:** `cmd/server/main-server.go:135-162` (`grabAndRemoveEnvVars`); no test file in `cmd/server/`; `go test -cover` reports 0.0% of statements.
- **What's uncovered:** the dual-name env-var cleanup added for the rename — unsets both the new (`remotetermbase.WaveClientIdVarName` etc.) and legacy (`LegacyWaveClientIdVarName` etc.) forms of six session-scoped vars including `WaveJwtTokenVarName`/`LegacyWaveJwtTokenVarName`, "since a leaked pre-rename process could still have the old name set" (comment at line 144-146). Also uncovered: `authkey.SetAuthKeyFromEnv()` and `remotetermbase.CacheAndRemoveEnvVars()` call ordering and error propagation (lines 136-143).
- **Why it matters:** if the legacy-name unset is dropped (e.g. someone "cleans up" what looks like dead code from the rename), a leaked pre-rename `WAVETERM_JWT`-style var would survive into a spawned process's environment where a child process could read it — this is the same credential-leak class as COV-3's redaction fix, just via env inheritance instead of logs.
- **Suggested test:** set both new- and legacy-named vars via `os.Setenv` in a test, call `grabAndRemoveEnvVars()`, assert both `os.LookupEnv` return not-present for all six pairs. `authkey.SetAuthKeyFromEnv`/`CacheAndRemoveEnvVars` may need stubbing or an accepted side effect if they're not already test-friendly.
- **Estimated effort:** S — the function itself is a flat list of `os.Unsetenv` calls; main cost is figuring out how much of `authkey`/`remotetermbase` setup the test needs to tolerate.
- **Certainty:** traced to `go test -cover`'s 0.0% for `cmd/server` and direct read of main-server.go:135-162.

### COV-5 [HIGH] emain-platform.ts data-dir migration shim (0bc74b18, d6666d49) has zero test coverage — project-wide, not just scoped dirs
- **Path:** `emain/emain-platform.ts`, whole file. `npx vitest run --coverage` (istanbul provider) reports 0/210 lines, 0/31 functions, 0/126 branches covered, and `find emain -name "*.test.ts*"` returns nothing.
- **Denominator check:** this run instrumented 279 files project-wide (18 of them under `emain/`), confirming istanbul's default `coverage.all=true` scanned the whole source tree rather than only the two directories passed as vitest test-path filters — so this 0% is not an artefact of a narrow scan, it's a real project-wide zero for this file.
- **What's uncovered:** the entire one-time synchronous migration described in `0bc74b18`'s commit message — moving config dir, data dir, legacy pre-v0.8 combined home dir, and best-effort Electron userData subtree from `waveterm`-prefixed to `remoteterm`-prefixed paths; the per-root marker-file validation (settings.json for config, wave.lock for data/legacy-home); the independent per-root override-var evaluation and `no-migration-needed:override` marker write; and, per `d6666d49`, the migration-failure-surfacing fix and the dev-suffix bug fix that review flagged.
- **Why it matters:** this runs once, synchronously, on every user's first post-rename launch, before the rest of the app is up — a regression here either loses a user's existing config/data (moves to the wrong place or partially moves and leaves state split across old/new roots) or blocks startup entirely. It is exactly the kind of code where a bug is invisible in normal development (works once, the marker file then suppresses re-runs) and only surfaces on a real user's machine. Zero automated coverage means the only defence against a regression is manual first-launch testing, which is easy to skip since re-running the migration requires resetting local state.
- **Suggested test:** this needs the `fs`/`path`/`app.getPath` boundary mocked or a temp-dir harness (isolated from the live filesystem per project rules — do not touch real XDG paths). Cover: (a) fresh install, no legacy roots exist, no-op; (b) legacy config dir exists with settings.json, migrates to new path; (c) legacy data dir absent but legacy home (wave.lock) exists, migrates via the combined-home path; (d) override var set making source==destination for one root only, other roots still migrate independently; (e) a root's move fails partway, error surfaces per `d6666d49`'s fix rather than being swallowed; (f) dev-suffix path variant, per the same commit's fix.
- **Estimated effort:** L — needs new test infrastructure for this file (no existing pattern in the repo mocks Electron's `app.getPath`/filesystem for emain/ code), and six-plus scenarios across four roots.
- **Certainty:** traced directly to `npx vitest run --coverage` lcov output (`emain/emain-platform.ts lines:0/210 funcs:0/31 branches:0/126`) and `find emain -name "*.test.ts*"` returning no files.

### COV-6 [HIGH] remotetermconfig-model.ts: secrets management and backgrounds write path are almost entirely untested; widgets/backgrounds missing-file-vs-read-failure branches are gapped even where the happy path is covered
- **Path:** `frontend/app/view/remotetermconfig/remotetermconfig-model.ts`. Whole-file: 195/518 lines (37.6%), 16/69 functions (23.2%), 39/193 branches (20.2%) via `npx vitest run --coverage`.
- **What's uncovered — secrets (lines 508-651, `refreshSecrets`/`viewSecret`/`showSecret`/`saveSecret`/`deleteSecret`/`startAddingSecret`/`addNewSecret`):** every one of these functions is in the uncovered-line set from the lcov report (e.g. 523-526, 536-538, 561-562, 571-573, 588-589, 617-618, 642-647). No test file for secrets exists at all (only `.widgets.test.ts` and `generalcontent.test.ts` in this directory). The brief explicitly calls out "secrets save" as in-scope changed logic — `saveSecret` (line 560) has no test.
- **What's uncovered — backgrounds (lines 845-996, `readRawBackgroundsFile`/`persistBackgroundPatch`/`writeBackgroundPatch`/`applyBackgroundToTab`/`updateBackgroundOpacity`/`updateBackgroundBlendMode`/`addBackground`/`submitBackgroundAdd`):** near-total gap — 846-988 is essentially wall-to-wall uncovered per the line-range scan, despite `readRawBackgroundsFile`/`writeBackgroundPatch` being the direct structural twin of the widgets read/write-queue pair that `remotetermconfig-model.widgets.test.ts` does cover in detail. The widgets test's own header comment even says the "same unmerged-read rationale" applies to backgrounds (source comment at line 841-844) — that rationale is untested for backgrounds itself.
- **What's uncovered — missing-file vs read-failure distinction, even within the tested widgets path:** in `readRawWidgetsFile` (lines 731-763), the `FileInfoCommand` catch block (740-742, sets `errorMessageAtom` and returns `null` on a check-failure) and the JSON-parse/non-object-shape branch (754-761, sets a different error message and also returns `null`) are both in the uncovered set — only the `fileInfo.notfound → return {}` happy path and the successful-parse happy path are exercised by the existing 5 tests, which are all reorder/toggle/back-to-back-write scenarios, none of them a read failure.
- **Why it matters:** secrets handling touches credential material (the brief's own framing groups this with the JWT/redaction work) — an unmerged/untested `saveSecret`/`deleteSecret` regression could silently corrupt or drop a stored secret with nothing catching it. The read-failure vs missing-file distinction is the load-bearing behaviour change called out explicitly in the brief; if a regression collapsed the two paths (e.g. treated a transient read error as "file doesn't exist, start fresh" and returned `{}` instead of `null`+error), `writeWidgetPatch`/`writeBackgroundPatch` would happily write an empty merge back, discarding the user's existing widgets/backgrounds on a transient I/O error — and no test would fail.
- **Suggested test:** for secrets, a new `remotetermconfig-model.secrets.test.ts` following the existing widgets test's mock-atom pattern, covering save/delete/add success and RPC-failure paths. For backgrounds, mirror `remotetermconfig-model.widgets.test.ts` directly (it's structurally the same read-merge-write-queue shape) — reorder is N/A but toggle/opacity/blend-mode/add map onto the same pattern. For the missing-file-vs-read-failure gap specifically: add two cases to the widgets test — `FileInfoCommand` rejecting (assert `errorMessageAtom` set, distinct from the `notfound` case), and `FileReadCommand` returning non-JSON/array content (assert error set and no destructive write follows).
- **Estimated effort:** L overall (secrets + backgrounds are two new test files); the missing-file-vs-read-failure addition to the existing widgets test is S on its own and highest-value-per-effort — it directly targets the brief's named behaviour with a file/mock pattern that already exists.
- **Certainty:** traced to `npx vitest run --coverage` lcov per-line data for `remotetermconfig-model.ts` (DA: entries with hit-count 0) and to reading `remotetermconfig-model.widgets.test.ts` in full (5 tests, all reorder/toggle/happy-path, no read-failure or notfound-vs-error case).

## Tests that can't fail
<!-- appended as found -->

### TC-1 TestHandleSystemResumeSmoke asserts nothing
- **Path:** `pkg/jobcontroller/jobcontroller_test.go:509-531`.
- **Issue:** sets up a mock disconnected connection, calls `HandleSystemResume(ctx)`, and ends with only a comment ("mock connection remains in controller map... won't interfere with other tests"). There is no `t.Error`/`t.Fatal`/assertion of any kind on the outcome — not on `conn.Status`, not on whether a reconnect was attempted, nothing. It will pass identically whether `HandleSystemResume` does the right thing, does nothing, or panics-and-recovers. `HandleSystemResume` itself shows 61.8% coverage per `go tool cover -func`, so this test contributes to that number without contributing any actual verification.
- **Fix direction:** assert on an observable side effect — e.g. that `attemptAutoReconnect`/`connectInternal` was invoked (via a test hook, following the pattern already used for `hasRunningDurableJobsTestHook` and `connectInternalTestHook` elsewhere in this file, e.g. `TestOnConnectionUpRetryRecoversFailedJobs`), or that `conn.Status` transitions as expected within a timeout.

## Completion
**Status:** COMPLETE

**Measurement basis:**
- Go: `go test ./pkg/jobcontroller/... ./pkg/remote/conncontroller/... ./pkg/shellexec/... ./pkg/remotetermbase/... ./cmd/server/... -cover -coverprofile=...` — all packages with tests passed (jobcontroller 24.5%, conncontroller 47.1%); `pkg/shellexec`, `pkg/remotetermbase`, `cmd/server` genuinely have zero `_test.go` files (confirmed via `find`), not a tool failure.
- Frontend: `npx vitest run --coverage` (istanbul provider, already installed — not installed by this session), scoped to `frontend/app/view/remotetermconfig` and `frontend/app/view/sourcecontrol` test-path filters but instrumenting all 279 project source files (istanbul `coverage.all` default) — 51/51 tests passed, 0 failures, confirming the suite actually ran rather than just compiling.

**Findings tally:** 6 coverage gaps (COV-1..6: 3 HIGH, 2 MEDIUM, 1 HIGH-uncertain-on-scope/COV-4-MEDIUM — see individual certainty notes), 1 test-that-can't-fail (TC-1).

**Could not check:** did not re-diff `bb79c27d`/`d6666d49` line-by-line against current HEAD to rule out drift since those commits (COV-3 flagged UNCERTAIN on this basis, now partly resolved — `RedactSecret` confirmed live at current line numbers). Did not attempt to run coverage on Electron main-process code beyond the static lcov scan (no existing harness for mocking Electron APIs in this repo, so COV-5's L-effort estimate assumes new infrastructure is needed, not confirmed against a prototype). Did not check `data-dir migration shim`'s "legacy env handling in cmd/server/main-server.go" beyond `grabAndRemoveEnvVars` — did not verify whether other legacy-env logic exists elsewhere in that file.
