# conformance-auditor report (Round 4)
**Target:** RemoteTerm rebrand, `git diff 816fb3d8 cd464aff`, focus: R3-CONF-1 onboarding logo fix verify + wave-art sweep; R3-CONF-2 dialog string fix verify; R3-CONF-3/4 RENAME_PLAN.md amendments vs shipped code; RENAME_ALLOWLIST.md staleness check.
**Started:** 2026-09-22 (approx, no live clock available)
**Status:** IN PROGRESS

## Findings
<!-- appended one at a time, as found -->

### [MEDIUM] R4-CONF-1: Onboarding's fake `.bashrc` demo still teaches new users `alias wave="wsh"`
- **Category:** missing (Phase 1 onboarding grep-gate scope, same gate R3-CONF-1 was found under)
- **Source says:** RENAME_PLAN.md:627-639's Phase 1 verification requires `grep -rni "wave" frontend/app/onboarding ...` to return only three named exceptions (fork-attribution line, `.kilocode`/`aiprompts` code-identifier prose, the FOSSA link). RENAME_ALLOWLIST.md:93-130's "Flagged, deliberately NOT actioned" list is the applicable escape hatch for a known-and-deferred miss; it has no entry for this line.
- **Code does:** `frontend/app/onboarding/onboarding-command.tsx:122`, inside `EditBashrcCommand`'s fake bashrc content shown to every new user in the walkthrough: `alias wave="wsh"`. Live re-run of the plan's own grep (2026-09-22, `grep -rni "wave" frontend/app/onboarding frontend/app/modals ...`) surfaces this as a hit that is not the fork-attribution line, not `.kilocode`/`aiprompts` prose, and not the FOSSA link. It predates this diff (introduced in `822f05e3`, "Big Onboarding Updates") and was not touched by `425180d8`'s R3-CONF-1 fix, whose commit message ("logo demo shows the RemoteTerm logo, not Wave art") is narrower than the file's full "wave" residue the Phase 1 gate is scoped to catch.
- **Impact:** low — the line doesn't render old artwork or brand strings, only teaches a shell alias name. But it's still a first-run surface presenting "wave" as a recognised equivalent of the `wsh` command in a RemoteTerm-branded walkthrough, which is exactly the class of miss the plan's own gate exists to catch, and it's uncertain whether this is a deliberate nod to Wave Terminal's own historical `wave`->`wsh` CLI rename (unrelated to this fork's WaveTerm->RemoteTerm rebrand) or a genuine leftover — nothing in RENAME_PLAN.md or RENAME_ALLOWLIST.md says which.
- **Recommendation:** either rename the demo alias to something rebrand-neutral (e.g. `alias rt="wsh"` or drop the line) or add an explicit allowlist entry recording it as a deliberate nod to `wsh`'s own pre-fork history, so the Phase 1 gate's exception list stays exhaustive rather than silently incomplete.

### [LOW] R4-CONF-2: RENAME_ALLOWLIST.md's flagged image-asset entry no longer accurately describes what's shipped
- **Category:** divergent (doc staleness, not a functional gap)
- **Source says:** RENAME_ALLOWLIST.md:115-120: "Load-bearing old-brand image assets (`assets/wave-{dark,light}.png`, `assets/wave-screenshot.webp`, `public/logos/wave-*.png`, `tsunami/frontend/public/wave-logo-256.png`): per Prerequisites decision 8, these need real new RemoteTerm artwork, not a rename — no design assets exist for these specific paths yet (**only `frontend/app/asset/logo.svg`/`logo-tile.svg` have shipped, for different use sites**)."
- **Code does:** as of `425180d8`, `public/logos/remoteterm-logo.png` (44,952 bytes, added this diff, `git diff 816fb3d8 cd464aff --stat`) is real new RemoteTerm artwork that has shipped, consumed by `frontend/app/onboarding/onboarding-command.tsx:7,111-112`. The four specific flagged paths themselves (`assets/wave-{dark,light}.png`, `assets/wave-screenshot.webp`, `public/logos/wave-*.png`, `tsunami/frontend/public/wave-logo-256.png`) are still present and still unreplaced (`ls public/logos/` and `ls assets/*.png assets/*.webp`, run live 2026-09-22, confirmed) — so the entry's core claim (these specific paths lack real art) is still true, but its parenthetical inventory of "what has shipped" is now incomplete by one asset.
- **Impact:** none functionally; a future contributor reading the allowlist to find all shipped RemoteTerm art would miss `remoteterm-logo.png`.
- **Recommendation:** append `public/logos/remoteterm-logo.png` (onboarding logo demo) to the parenthetical the next time this entry is touched.

## Verified OK
<!-- appended as checked -->

- **R3-CONF-1 fully fixed and test-pinned.** `frontend/app/onboarding/onboarding-command.tsx:7` now imports `remoteTermLogo from "/logos/remoteterm-logo.png"`; `:111-112`'s `ViewLogoCommand` shows command string `wsh view public/remoteterm-logo.png` and label `remoteterm-logo.png`. New test `frontend/app/onboarding/onboarding-command.test.tsx` renders `ViewLogoCommand`, asserts the typed command text, asserts the rendered `img` `src` does not match `/wave/i`, and asserts the referenced file exists on disk. Repo-wide sweep of `frontend/app` and `emain/*.ts` (excluding `*.test.ts`) for `wave-logo`/`/logos/wave`/`wave-ribbon` found no other runtime import outside the already-fixed R2-CONF-1 window-icon path and the already-allowlisted `tsunami/frontend/`/`docs/`/`README*.md`/`frontend/preview/` surfaces (dev tooling and separately-scoped per RENAME_PLAN.md/RENAME_ALLOWLIST.md, not re-litigated here).
  - `frontend/preview/index.html:8` `<link rel="icon" type="image/png" href="/logos/wave-logo-256.png" />` — dev-only preview-server favicon, per brief listed not counted.
  - `frontend/preview/mock/mockfilesystem.ts:296` — mock filesystem test fixture path string, not a rendered asset; dev tooling.
- **R3-CONF-2 fully fixed.** `emain/emain-platform.ts:266-267` (`"Wave Terminal Is Still Running"` / `"Quit Wave Terminal, then relaunch RemoteTerm."`) and `:279-284` (`"Wave Terminal May Still Be Running"` / `"RemoteTerm could not confirm that Wave Terminal has quit."`) now say "Wave Terminal" throughout, matching the legacy app's own menu string and RENAME_PLAN.md's established display-name convention. `emain/emain-platform.test.ts:349` area re-pinned to match. One internal, non-user-visible `console.log` at `:289` ("user chose to migrate despite a possibly running WaveTerm") still says "WaveTerm" but is a debug log line, not a dialog string, so it's outside R3-CONF-2's scope and not re-flagged.
- **R3-CONF-3 and R3-CONF-4 fully reconciled — RENAME_PLAN.md's 2026-09-22 amendments match shipped code line-for-line.** Verified each specific claim in the two amended blocks (`git diff 816fb3d8 cd464aff -- RENAME_PLAN.md`) against `emain/emain-platform.ts` and `emain/emain.ts` at `cd464aff`:
  - Config root "always merges into the existing destination" -> `emain-platform.ts:488` `canMergeIntoDest: () => true`.
  - Data root merges "only if every top-level destination entry is" `logs/`/`rtapp.log` plus a same-directory Electron userData dir -> `emain-platform.ts:116` `EmainDataEntryNames = new Set(["logs", "rtapp.log"])`, `:494-500` extends that set with the Electron userData basename only `if (path.dirname(electronUserData) === dataDest)`, `:508` `canMergeIntoDest: (destEntries) => destEntries.every((name) => blockedLaunchDataEntryNames.has(name))`.
  - Legacy-home root "still aborts loudly" -> `emain-platform.ts:514-520`'s `migrateDataRoot` call has no `canMergeIntoDest`, so it falls through to the original abort branch (`:349-353`).
  - "Merge never overwrites... Electron's `electron/` profile kept whole" -> `mergeTree` (`:386-406`) only moves entries the destination lacks, and `UnmergeableDirNames = new Set(["electron"])` (`:382`) is checked before recursing.
  - "Resumable... writes `.migrating-from-waveterm`... removes it after the completion marker" -> `MigrationInProgressFileName = ".migrating-from-waveterm"` (`:122`), written at `mergeDataRoot`'s start (`:416`) and `unlinkSync`'d after the marker write (`:430`).
  - SingletonLock path, identity check (`/proc/<pid>/exe` on Linux, `ps -p <pid> -o comm=` on macOS), dev/prod flavour scoping, and the two dialog texts/button sets ("Wave Terminal Is Still Running"/Quit-only vs "Wave Terminal May Still Be Running"/Quit-or-Migrate-anyway) -> `checkLegacyInstanceRunning` (`:196-241`) and `resolveLegacyInstanceBlock` (`:256-294`) match the amendment's description clause for clause, including the macOS `app.setName(ElectronUserDataPath.join("/"))` (`:30`, `ElectronUserDataPath = ["remoteterm", "electron"]` at `:29`) that the amendment cites for where Electron's userData lands.
  - The ordering claim ("awaits `resolveLegacyInstanceBlock()`... before the server starts... quitting leaves the migration to retry") -> `emain/emain.ts:274-284`, `resolveLegacyInstanceBlock()` awaited and gated with `electronApp.quit(); return;` before the `runRemoteTermSrv()` call at `:284`, matching comment at `:272-273`.
  - Regression test claim ("`emain-startup-order.test.ts` fails if `appMain()` stops awaiting that call or returning on `false` before `runRemoteTermSrv()`") -> `emain/emain-startup-order.test.ts:53-68` parses `emain.ts`'s AST and asserts the awaited-guard-that-returns statement index is less than the `runRemoteTermSrv` call's statement index. Test exists and does what the amendment claims.
  - No mismatches found in either amendment against the shipped code.
- **RENAME_ALLOWLIST.md's flagged-not-actioned list (RENAME_ALLOWLIST.md:93-130) still substantively reflects reality**, checked item by item against the current tree: `WaveEnv`/`useWaveEnv` family still present and unrenamed (`frontend/app/remotetermenv/remotetermenv.ts` and 48 consumers, spot-checked); `.github/FUNDING.yml` still `github: wavetermdev`; `docs/docs/*.mdx` untouched; `onboarding.tsx:116`'s "Join the Wave Discord Channel" still present, same Discord URL; the four load-bearing old-brand image asset paths still unreplaced (see R4-CONF-2 for the one stale detail in this entry's own text); `wsh`/`wps` backronyms unchanged; `.gitignore`'s `restart_waveterm.png` entry unchanged. Nothing on this list was silently resolved without the allowlist being updated, and nothing icon/onboarding-fix-adjacent needed removing from it (R3-CONF-1's fix used a new dedicated PNG, not the flagged `logo.svg`/`logo-tile.svg` assets, so it doesn't retire any bullet on this list — R4-CONF-2 documents the resulting drift).

## Completion
**Status:** COMPLETE
**Tally:** MEDIUM: 1 (R4-CONF-1) | LOW: 1 (R4-CONF-2) | Verified OK: 4 major areas (R3-CONF-1, R3-CONF-2, R3-CONF-3/4 plan amendments, RENAME_ALLOWLIST.md flagged list)
Everything in scope was checkable from the repo (grep, blob/file reads, git log/diff) without launching the app or touching the live system.
