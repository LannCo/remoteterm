# round2-security-auditor report
**Target:** `git diff 35cd6146 75b60911` on `qa/fleet-2026-09-22` (fix-wave review)
**Started:** 2026-09-22T00:00:00Z
**Status:** IN PROGRESS

## Findings
<!-- appended one at a time, as found -->

_None._

## Verified OK
<!-- appended as checked -->

- `pkg/wshrpc/wshremote/git.go:311-343` (`GitRevertHunkCommand`) + `:1078-1140` (`extractHunkPatch`, `applyPatchReverse`): the hand-inverted-patch approach was replaced with `git apply -R` on the *forward* hunk text. Patch content is written to `cmd.Stdin` (`strings.NewReader(patch)`), never to a temp file or a shell string, and the `git apply -R` argv is a fixed two-element slice — `data.Path` (attacker-influenced repo-relative path) only appears inside the patch body's `--- a/%s`/`+++ b/%s` headers, not as a process argument, so there is no argument-injection or path-traversal-via-flag vector introduced by this change. Same shape as the pre-existing `applyPatchCached` (`git apply --cached`) already on this code path. `cmd.Dir = data.Dir` is unchanged from before this diff (existing trust boundary: the RPC caller already controls which repo dir operations run against via the authkey-gated `wshrpc` channel, same as every other git command in this file — not a new surface).
- `pkg/remotetermbase/wavebase.go:170-202` (`AcquireWaveLock`, `multiFDLock`) + `wavebase-posix.go`/`wavebase-win.go` (`acquireLockFile` extraction): second flock is only attempted when `os.Stat(legacyLockFileName)` finds `wave.lock` present, and `acquireLockFile` opens it with the same `O_RDWR|O_CREATE, 0600` (posix) / `filemutex.New` (win) as the primary lock — no permission widening, no `O_CREATE` on a path that didn't already exist unexpectedly (the file must already exist for the branch to trigger, since `os.Stat` gates it). This is the identical symlink-following pattern the pre-existing primary `WaveLockFile` open already has (not introduced by this diff) — extending it to a second, hard-coded filename inside the same already-trusted data dir doesn't create a new attacker capability: exploiting it would require the same "attacker can already write inside your data dir" precondition SEC-1 (round 1) covers for the migration shim, not a new one. On failure to take the legacy lock, `lock.Close()` releases the first fd (`wavebase.go:194`) — no fd leak on the error path. `multiFDLock.Close()` (`:174-182`) closes both in reverse-acquisition order and joins errors — no leak, no double-free.
- `pkg/rtconfig/settingsconfig.go:439-450,717-775` (lock now spans read-modify-write in `SetBaseConfigValue`/`SetConnectionsConfigValue`): confirmed `ReadWaveHomeConfigFile` (`:433`) does not itself take `configWriteLock` (it's a plain file read), so the new `configWriteLock.Lock()`/`defer Unlock()` at the top of `SetBaseConfigValue`/`SetConnectionsConfigValue` followed by a call to `ReadWaveHomeConfigFile` is not a self-deadlock (`sync.Mutex` is non-reentrant, and no nested `Lock()` occurs). Grepped every caller of `ReadWaveHomeConfigFile`/`SetBaseConfigValue`/`SetConnectionsConfigValue`/`WriteWaveHomeConfigFile` (`wslconn.go`, `connutil.go`, `wshserver.go`, `conncontroller.go`) — none holds any other named lock before calling in, so no lock-ordering/deadlock risk with a second lock elsewhere in the codebase. `writeWaveHomeConfigFileLocked` is unexported with a "caller must hold configWriteLock" comment and both call sites hold it — correctly threaded.
- `emain/emain-platform.ts:192-208` (`legacyConfigSkipReason`) skip-reason logging (`:138-142`, `console.log`): logs only the fixed root name, `spec.source` path (already user-visible/known), and a static reason string (`"has no config files (*.json or presets/)"`, `"could not be read (%e)"`, `"has no wave.lock"`) — no file contents, secret values, or directory listing are logged. `readdirSync` failure is logged via `${e}` (Node `Error.toString()`, i.e. `Error: ENOENT: ... path`) — same path already being logged one line prior, not a new disclosure.
- `frontend/app/view/remotetermconfig/secretscontent.tsx` a11y changes (`useId`-generated `nameId`/`nameHintId`/`valueId`, `aria-describedby`, `htmlFor`): the only text placed at the new `aria-describedby` target is the static hint string ("Must start with a letter..." / "Invalid name. ...") — never `newSecretName`, `secretValue`, or any atom holding secret data. `valueRef` callback (`useCallback`) only assigns `model.secretValueRef` and calls `.focus()`; no new read/serialization of the secret value.
- `pkg/jobcontroller/jobcontroller.go:162,930-940` (`resumeReconnectTestHook`): unexported (lowercase) package-level var; only assignment site is `pkg/jobcontroller/jobcontroller_test.go:536,543`, a `_test.go` file excluded from production builds by the Go toolchain — not settable outside `go test` for this package. Same declared-nil-by-default shape as the pre-existing `isConnectedTestHook`/`reconcileOnUpTestHook`/etc. in the same var block.
- `pkg/jobcontroller/jobcontroller.go:2028-2058` (`doReconnectJobExclusive`, per-job `jobReconnectLocks` mutex serialising `ReconnectJob`/`ReconnectJobRoute`): auth/authz-neutral concurrency fix, no new external input handled: `jobId` is server-derived, not attacker-suppliable format. Not a security-relevant surface change beyond what round-1 CA-7 already covered.
- `emain/emain-platform.ts:192-208` (`legacyConfigSkipReason`, CA-6 fix) widens what counts as a valid legacy config root from "has `settings.json`" to "has any top-level `*.json` or a `presets/` dir" — this does lower the bar for round-1 SEC-1 (config-root migration follows a symlink at `spec.source`; `validateSource`/`sourceSkipReason` was already noted in SEC-1 as not a real defense against that, only raising the bar for what the attacker's target must contain). Re-checked against SEC-1's own conclusion: SEC-1's recommended fix (`lstatSync` + refuse on `isSymbolicLink()`) is orthogonal to and unaffected by this widening — the skip-reason check still runs only after `existsSync(spec.source)`, which follows symlinks the same as before, so this is a quantitative easing of an already-open, already-reported gap, not a new independent vulnerability. Not re-filed as a new finding; flagged here so it's not lost — SEC-1's fix should close both the pre- and post-diff shape of this gap.

## Completion
**Status:** COMPLETE
**Findings:** 0 (0 Critical, 0 High, 0 Medium, 0 Low)
**Not checked / out of scope:** No live-app/runtime verification (live-system rule) — `git apply -R`, the second flock, and the migration skip-reason paths are reasoned from the diff and `go test` results, not exercised against a running instance. Did not re-audit `frontend/app/view/remotetermconfig/generalcontent.tsx`, `connectionscontent.tsx`, `backgroundscontent.tsx`, `widgetscontent.tsx`, or `remotetermconfig-model.ts` beyond the secrets-tab hunk named in the brief — those are a11y/UX fixes with no secret-handling or input-validation surface per their diff summary, and were left to `r2-a11y`/`r2-code`.
