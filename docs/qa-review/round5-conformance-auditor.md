# conformance-auditor report (Round 5, FINAL)
**Target:** RemoteTerm rebrand, `git diff 60f923d2 72b21bd9`, focus: R4-CONF-1 onboarding alias fix verify + re-run Phase 1 grep gate; R4-CONF-2 allowlist staleness fix verify; RENAME_PLAN.md amendments for R4-CA-5/6/7 vs `emain/emain-platform.ts`/`emain/emain.ts`; whole-branch residual Wave/waveterm/wavesrv sweep.
**Started:** 2026-09-22
**Status:** IN PROGRESS

## Findings
<!-- appended one at a time, as found -->

### [MEDIUM] R5-CONF-1: Re-running the Phase 1 onboarding grep gate finds a second stale product-name reference, inside a file the previous fix commit touched
- **Category:** missing (Phase 1 onboarding grep-gate scope, same gate R3-CONF-1/R4-CONF-1 were found under)
- **Source says:** RENAME_PLAN.md:627-639's Phase 1 gate; RENAME_PLAN.md:537-539's own risk note for the version-locked onboarding-upgrade files: "don't rename the *files* ... only the strings inside change."
- **Code does:** `frontend/app/onboarding/onboarding-upgrade-v0144.tsx:67,70`: `<strong>Config Errors Moved</strong> - Config errors to the WaveConfig view for less clutter` and `<li>WaveConfig now warns on Unsaved Changes</li>`. The current view is `frontend/app/view/remotetermconfig/` (renamed before this diff range). Commit `034ac348` ("rename(phase1): rebrand core user-visible UI strings to RemoteTerm") edited this exact file, changing "Wave v0.14.4" -> "RemoteTerm v0.14.4" on line 8, but left "WaveConfig" untouched two lines away — so this isn't a deliberate "historical accuracy" choice (the surrounding prose in the same commit was already rebranded), it's a miss. Live re-run of the gate (2026-09-22) surfaces it; not one of the gate's three named exceptions.
- **Impact:** low-to-moderate — shown only to users upgrading through the v0.14.4 release-note screen, but it's the same class of self-inflicted miss as R3-CONF-1/R4-CONF-1: the plan's own verification step should have caught it, and a prior fix commit edited this precise file without noticing it.
- **Recommendation:** change "WaveConfig" to "RemoteTermConfig" or a generic "the Config view" in both lines, consistent with how the same commit already rebranded the surrounding sentence.

### [LOW] R5-CONF-2: Onboarding's fake build-progress animation still names the pre-rename Go binary
- **Category:** missing (same gate, `frontend/app/onboarding` scope)
- **Source says:** RENAME_PLAN.md:627-639 gate; the Go server binary and process are now named `remotetermsrv` throughout the current tree (`Taskfile.yml:145-261`, `emain/emain.ts:42,181,197,203,295,309` all say `remotetermsrv`).
- **Code does:** `frontend/app/onboarding/onboarding-layout-term.tsx:80`, `deployMessages[2]`: `"[3/8] Building Go backend (wavesrv)..."`, shown in a fake terminal deploy animation on the Durable Sessions onboarding screen (added `4983ebc9`, not a version-locked file, so the historical-preservation exception doesn't apply here).
- **Impact:** cosmetic — an 8-step fake progress animation shown once during onboarding names a binary that hasn't been called `wavesrv` in this fork for several phases.
- **Recommendation:** change to `"[3/8] Building Go backend (remotetermsrv)..."`.

### [HIGH] R5-CONF-3: The wsh-install confirmation dialog shown on every new SSH/WSL connection still says "Wave"
- **Category:** missing (whole-branch residual sweep, `pkg/` user-facing messages)
- **Source says:** RENAME_PLAN.md's established display-name convention (e.g. the R3-CONF-2 dialog fix) is to rebrand user-visible dialog strings to "RemoteTerm"/"RemoteTerm Terminal" wording. Neither RENAME_PLAN.md nor RENAME_ALLOWLIST.md mentions this dialog anywhere (grepped both for `wslconn`, "Install Wave Shell", "Shell Extensions" — no hits).
- **Code does:** `pkg/wslconn/wslconn.go:352-358,380,387`: `queryTextTemplate` = `"Wave requires Wave Shell Extensions to be installed on %q to ensure a seamless experience.\n\nWould you like to install them?"`, dialog `Title: "Install Wave Shell Extensions"`, confirm button `OkLabel: "Install wsh"`. This is a real `userinput.UserInputRequest` confirm dialog (`getPermissionToInstallWsh`, called from `EnsureConnection` at `:573`), shown to the user the first time they connect to a WSL (and, per git history at `668a8eda`, formerly also SSH) connection that doesn't have `wsh` installed yet and hasn't opted into auto-install.
- **Impact:** this is a routine, high-visibility dialog (every first connection to a new remote/WSL target that needs `wsh`) that names the pre-rename brand twice in the body text and once in the title — a materially bigger miss in reach than the onboarding-screen strings this round's other findings concern, since it fires during normal day-to-day use, not just first-run onboarding.
- **Recommendation:** rebrand both the title and query text to "RemoteTerm", e.g. title `"Install RemoteTerm Shell Extensions"`, body `"RemoteTerm requires RemoteTerm Shell Extensions to be installed on %q..."` (or drop the redundant second "Wave"/"RemoteTerm" per plain-English editing).

### [LOW] R5-CONF-4: The dev-build indicator's own tooltip still says "Wave"
- **Category:** missing (whole-branch residual sweep, `frontend/app`)
- **Source says:** same display-name convention as above; not covered by RENAME_PLAN.md or RENAME_ALLOWLIST.md (grepped for "Dev Build", "Running Wave" — no hits).
- **Code does:** `frontend/app/workspace/widgets.tsx:620` and `:664` (two render paths of the same widget): `<div ... title="Running Wave Dev Build"><LogoTile /></div>`, rendered whenever `env.isDev()` is true.
- **Impact:** low — visible only to people running a dev build (this project's own contributors, per `[[project_remoteterm_rename]]`'s daily-driver context), but it's a native hover tooltip on the app's own logo tile, ironic given it's specifically labelling *this* app's dev build.
- **Recommendation:** change both occurrences to `"Running RemoteTerm Dev Build"`.

### [LOW] R5-CONF-5: Two rare-path shell-init error messages, echoed into the user's terminal, still say "Wave"
- **Category:** missing (whole-branch residual sweep, `pkg/` user-facing messages)
- **Source says:** same convention; not covered by RENAME_PLAN.md/RENAME_ALLOWLIST.md.
- **Code does:** `pkg/blockcontroller/shellcontroller.go:830,835`: `fmt.Sprintf("echo \"cannot expand home dir in Wave initscript file, from key %s\";\n", metaKeyName)` and `fmt.Sprintf("echo \"cannot open Wave initscript file, from key %s\";\n", metaKeyName)`. Both are returned as the block's init script, so `echo` runs in the user's own terminal block on failure, printing "Wave initscript file" directly into their terminal.
- **Impact:** low — only triggers when a configured custom init-script file path can't be expanded or read, but when it does, the exact wording naming "Wave" prints straight into the user's terminal.
- **Recommendation:** reword to "cannot expand home dir in the custom init script file" / "cannot open the custom init script file" (drop the brand reference rather than swap it, since "initscript" isn't itself a RemoteTerm/Wave-branded feature name elsewhere in the codebase).

### [LOW] R5-CONF-6: SSH ProxyJump depth-limit error names "Wave" instead of the product
- **Category:** missing (whole-branch residual sweep, `pkg/` user-facing messages)
- **Source says:** same convention; not covered by RENAME_PLAN.md/RENAME_ALLOWLIST.md.
- **Code does:** `pkg/remote/sshclient.go:1156`: `utilds.Errorf(ConnErrCode_ProxyDepth, "ProxyJump %d exceeds Wave's max depth of %d", jumpNum, SshProxyJumpMaxDepth)`, wrapped in a typed `ConnectionError` with its own error code, the shape used elsewhere in this package to surface connection failures to the frontend's connection-status UI.
- **Impact:** very low reach (only hit by an SSH `ProxyJump` chain deeper than `SshProxyJumpMaxDepth`), but the message is user-facing connection-error text, not a log line.
- **Recommendation:** reword to `"ProxyJump %d exceeds the max depth of %d"` (drop the brand reference) or `"...exceeds RemoteTerm's max depth..."` for consistency with other rebranded error strings.

## Verified OK
<!-- appended as checked -->

- **R4-CONF-1 fully fixed.** `frontend/app/onboarding/onboarding-command.tsx:122`: `alias rt="wsh"` (was `alias wave="wsh"`). Test `onboarding-command.test.tsx:47-56` pins `"first-run .bashrc demo aliases wsh as rt, not wave"`, asserting the rendered text does not match `/\bwave\b/i`.
- **Live re-run of RENAME_PLAN.md:629-631's exact gate command** (`grep -rni "wave" frontend/app/onboarding frontend/app/modals .github/ISSUE_TEMPLATE .github/copilot-instructions.md .github/FUNDING.yml .kilocode/skills .kilocode/rules electron-builder.config.cjs pkg/wconfig/defaultconfig/settings.json README*.md BUILD.md CONTRIBUTING.md ACKNOWLEDGEMENTS.md ROADMAP.md AGENTS.md CLAUDE.md RELEASES.md IMAGE-RENDERING-INVESTIGATION.md REMOTE-IMAGE-PASTE-SPEC.md aiprompts`, 2026-09-22): all hits inside `frontend/app/onboarding` and `frontend/app/modals` accounted for — the R5-CONF-1 miss above; the already-allowlisted `useWaveEnv`/`waveEnv` code identifier (`onboarding-upgrade-v0140.tsx:4,7`, `onboarding-upgrade-v0142.tsx:4,7`) and "Join the Wave Discord Channel" (`onboarding.tsx:116`, `RENAME_ALLOWLIST.md:110-113`); test-file assertions/mocks referencing "wave" as the thing *not* shown (`onboarding-command.test.tsx`); and `frontend/app/modals/userinputprompt.tsx:90-95`'s `waveEvent` local variable (a keyboard-event identifier, not brand text, pre-existing and out of the gate's three exceptions but also not "Wave" the brand — same class as other code-identifier misses already on `RENAME_ALLOWLIST.md`'s deferred list, not re-flagged as new). No new hits outside these.
- **R4-CONF-2 fully fixed and accurate.** `RENAME_ALLOWLIST.md:115-120`'s parenthetical now reads "only `frontend/app/asset/logo.svg`/`logo-tile.svg`, `public/logos/remoteterm-logo.png` (onboarding) and the `build/icons/*` renders have shipped". Confirmed `build/icons/*.png` (7 files, `16x16` through `512x512`) exist and were added by `e7a30ef8` ("feat(build): app icons rendered from RemoteTerm logo mark") — the added claim is accurate, not just plausible-sounding.
- **RENAME_PLAN.md amendments for R4-CA-5/6/7 all match the shipped code, no mismatches found:**
  - **R4-CA-7 (dev-flavour identity match):** amendment says a dev-flavour lock holder is "never confirmed running, only cannot be ruled out (Quit / Migrate anyway)" and "only the production basenames get the Quit-only dialog." Code: `emain/emain-platform.ts:270-276` — `if (holderIsDev) return { confirmedRunning: false, ... }` unconditionally, regardless of whether the pid is really the legacy dev build or an unrelated Electron process with a reused pid. `resolveLegacyInstanceBlock()` (`:308-346`) branches on `block.confirmedRunning`, so only the `false` (Quit/Migrate-anyway) path is reachable for a dev-flavour holder.
  - **R4-CA-5 (dev/prod profile scoping):** amendment says the shared legacy profile "sits inside the production Linux config root and macOS data root," so "a root that contains the profile is blocked by a holder of either flavour" while "a root that does not contain it migrates without a dialog when the holder is the other flavour," and "a dev build with a production holder migrates everything." Code: `legacyInstanceBlocksRoot()` (`emain-platform.ts:282-293`) returns `true` (blocks) unless `state.otherFlavour` and the profile dir (`<appData>/waveterm/electron`) is *not* inside `source`; called from `migrateDataRoot()` (`:375-382`) per root. For a dev build the profile is never inside either `-dev` root, so `otherFlavour` (prod holder) never blocks — matches "migrates everything."
  - **R4-CA-6 (incomplete-migration / `db/`-both-sides gate):** amendment adds "the server does not start after a failed or incomplete migration," naming `resolveIncompleteMigrationBlock()`, shown as a "RemoteTerm Data Migration Issue" error box, and states a "sticky abort" (non-empty, unmarked destination) and a completed merge that kept destination copies do *not* block. Code: `resolveIncompleteMigrationBlock()` (`emain-platform.ts:161-173`) exists verbatim, title `"RemoteTerm Data Migration Issue"` (`:167`); wired in `emain/emain.ts:275,280,290` between `resolveLegacyInstanceBlock()` and `runRemoteTermSrv()`. The "sticky abort" path (`emain-platform.ts:402-406`, non-empty unmerge-able destination) calls `recordMigrationFailure` (not `recordIncompleteMigration`), so `migrationIncomplete` stays `false` and `resolveIncompleteMigrationBlock()` returns `true` (no block) — matches. The `db/`-on-both-sides case (`mergeDataRoot`, `:472-479`) calls `recordIncompleteMigration` with both paths named and tells the user to move the destination's `db/` aside if it holds nothing needed — matches the amendment's wording closely.

## Completion
**Status:** COMPLETE
**Tally:** HIGH: 1 (R5-CONF-3) | MEDIUM: 1 (R5-CONF-1) | LOW: 4 (R5-CONF-2, R5-CONF-4, R5-CONF-5, R5-CONF-6) | Verified OK: 3 major areas (R4-CONF-1 fix + gate re-run, R4-CONF-2 fix, RENAME_PLAN.md R4-CA-5/6/7 amendments)
Everything in scope was checkable from the repo (grep, blob/file reads, git log/diff) without launching the app or touching the live system. Not checked: real macOS `ps -o comm=` behaviour, Chromium `requestSingleInstanceLock` runtime behaviour (both pre-existing UNVERIFIED items from round 4, not re-verified this round since neither had a code change in `60f923d2..72b21bd9`).
